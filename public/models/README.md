# 3D models (maquettes)

Drop `.glb` files here and reference them from a project's **Media → 3D Model**
field as `models/<name>.glb`. Keep them lean: aim for < 5 MB, Draco/meshopt
compression welcome. The site lazy-loads the viewer only for projects that
have a model.
